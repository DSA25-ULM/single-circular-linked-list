#include <conio.h>
#include <iostream>
#include <stdlib.h>

using namespace std;


// write the definition (body) of the single circular linked list here using struct and pointer
// the elements of the stack are string

// write the function to implement all the operation needed
// push front n back, pop front n back, isEmpty, isFull, display, reset, init, find element, add after or before a specific element

// write the menu function in main scope
// to insert, delete, display, reset, exit

int main(){

}